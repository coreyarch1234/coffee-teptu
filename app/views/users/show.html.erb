<div class="result-wrapper col-md-8 col-md-offset-2">
  <table class="table table-filter">
    <tbody>
      <tr>
        <td>
          <div class="media">
            <div class="col-md-2">
              <%= link_to image_tag('https://s3.amazonaws.com/uifaces/faces/twitter/fffabs/128.jpg'), @user, :class=>"pull-left"%>
            </div>
            <div class="col-md-10">
              <h3><%= "#{@user.first_name} #{@user.last_name}" %></h3>
              <h3><%= @user.coffee_title %></h3>
              <hr />
              <h4><%= @user.about_me %></h4>
              <hr />
              <h5><%= @user.contact %></h5>
              <% current_user.projects.each do |p| %>
                <hr />
                <h2><%= link_to p.title, user_project_path(@user, p) %></h2>
              <% end %>
              <p><%= link_to 'Add a Project', new_user_project_path(@user) %></p>
              <%= link_to 'Back', users_path %>
              <% if current_user == @user %>
                <%= link_to 'Edit', edit_user_path(@user) %>
              <% end %>
            </div>
          </div>
        </td>
      </tr>
    </tbody>
  </table>
<div>

<style>
    /*Profile Picture Movement Up*/
    .col-md-2{
        margin-right: 50px;
    }

    /*About Me Movement Down*/
    .col-md-10 > h4{
        margin-top: 30px;
    }


</style>
